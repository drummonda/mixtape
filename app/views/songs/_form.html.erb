<%= form_with(model: song, local: true) do |form| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :song_title %>
    <%= form.text_field :song_title, id: :song_song_title %>
  </div>

  <div class="field">
    <%= form.label :song_artist %>
    <%= form.text_field :song_artist, id: :song_song_artist %>
  </div>

  <div class="field">
    <%= form.label :playlist_id_id %>
    <%= form.text_field :playlist_id_id, id: :song_playlist_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
