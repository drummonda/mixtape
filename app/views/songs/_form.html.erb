<%= form_with(model: song, local: true) do |form| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :song_title, id: :song_title, class: 'form-control', placeholder: "Song Title" %>
  </div>

  <div class="field">
    <%= form.text_field :song_artist, id: :song_artist, class: 'form-control', placeholder: "Song Artist" %>
  </div>

  <div class="field">
    <%= form.file_field :mp3, class: 'form-control', placeholder: 'Upload mp3 here' %>
  </div>
  <br>
  <div class="actions">
    <%= form.submit 'Upload Song', class: 'btn btn-primary btn-sm float-left' %>
  </div>
  <br>
  <br>
  <br>
<% end %>

