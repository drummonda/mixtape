<%= form_with(model: playlist, local: true) do |form| %>
  <% if playlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(playlist.errors.count, "error") %> prohibited this playlist from being saved:</h2>

      <ul>
      <% playlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :playlist_name %>
    <%= form.text_field :playlist_name, id: :playlist_playlist_name %>
  </div>

  <div class="field">
    <%= form.label :creator_id_id %>
    <%= form.text_field :creator_id_id, id: :playlist_creator_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
